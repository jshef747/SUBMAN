apiVersion: v1
kind: Service
metadata:
  name: subman-client
  namespace: {{ .Release.Namespace }}
spec:
  type: LoadBalancer
  selector:
    app: subman-client
  ports:
    - port: 80
      targetPort: 80
